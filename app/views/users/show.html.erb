<% provide(:title, "#{@user.name}の詳細") %>
<h2><%= "#{@user.name}さんのページ" %></h2>
<div class="row">
  <div class="created-events col-md-6 col-xs-6">
    <%= render 'created_events' %>
  </div>
  <div class="participated-events col-md-6 col-xs-6">
    <%= render 'participated_events' %>
  </div>
</div>
